# All Kafka topics we want this project to have.
# Edit here → run `make create-topics` to apply (idempotent creation).

topics:
  - name: smoke.test
    partitions: 1
    replication_factor: 1
    config:
      retention.ms: "604800000"
      cleanup.policy: "delete"

  - name: trades.raw
    partitions: 3
    replication_factor: 1
    config:
      retention.ms: "604800000"
      cleanup.policy: "delete"

  - name: trades.raw.dlq
    partitions: 1
    replication_factor: 1
    config:
      retention.ms: "1209600000"
      cleanup.policy: "delete"

  - name: trades.silver
    partitions: 3
    replication_factor: 1
    config:
      retention.ms: "1209600000"
      cleanup.policy: "delete"
